extends: final-hemera-nano.yaml
name: hemera-final-real-preflight
data:
  cache_dir: preflight-final/cache
  max_items: 32
  shard_size: 32
train:
  output_dir: preflight-final/train
  seed: 2026
  steps: 20
  batch_size: 16
  effective_batch_size: 16
  learning_rate: 0.0002
  warmup_ratio: 0.1
  ema_half_life_images: 256
  log_every: 1
  sample_every: 0
  save_every: 20
budget:
  max_eur: 0
  hourly_eur: 0.4905
  reserve_minutes: 0
  save_milestones: false
eval:
  samples: 16
  batch_size: 16
  num_inference_steps: 4
  guidance_scale: 3
